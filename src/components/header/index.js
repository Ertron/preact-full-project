import { h, Component } from 'preact';
import { Link } from 'preact-router';
import style from './style';

export default () => (
	<header class={style.header}>
		<h1>Test Preact App</h1>
	</header>
);
